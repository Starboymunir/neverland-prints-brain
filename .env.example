# ===== Neverland Prints Brain — Environment Variables =====

# ---------- Supabase ----------
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key

# ---------- Google Drive ----------
# Create a service account in Google Cloud Console,
# download the JSON key, and put the path here
GOOGLE_SERVICE_ACCOUNT_KEY_PATH=./credentials/google-service-account.json
# The Google Drive folder ID that contains your artwork
# (grab it from the URL: https://drive.google.com/drive/folders/<THIS_ID>)
GOOGLE_DRIVE_FOLDER_ID=1k_ZNb3_e1WHUlnDSlkrGggrYTvYDMWhD

# ---------- Shopify Admin API ----------
# New flow (Jan 2026+): Dev Dashboard → Create app → Settings → Client ID + Secret
# Tokens are auto-fetched via client credentials grant (expire every 24h)
SHOPIFY_STORE_DOMAIN=neverland-prints.myshopify.com
SHOPIFY_CLIENT_ID=your-client-id
SHOPIFY_CLIENT_SECRET=your-client-secret
SHOPIFY_API_VERSION=2024-10

# ---------- Google Gemini AI ----------
GEMINI_API_KEY=your-gemini-api-key

# ---------- Server ----------
PORT=3000
NODE_ENV=development

# ---------- Resolution Engine ----------
# 1 cm = 35.43 px  (client-specified conversion)
PX_PER_CM=35.43
# Minimum DPI for print quality
MIN_PRINT_DPI=150
